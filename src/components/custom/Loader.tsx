"use client"

const Loader = () => {
    return (
        <main className={`z-[100] bg-black flex items-center justify-center fixed w-screen h-screen `}>
            Loading...
        </main>
    );
};

export default Loader;

